require 'spec_helper'

describe PolicyMachineTestApp do
  it 'has a version number' do
    expect(PolicyMachineTestApp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
